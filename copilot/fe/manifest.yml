# The manifest for the "fe" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: fe
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  healthcheck:
    path: /
    success_codes: '200-399'
    interval: 30s
    timeout: 10s


network:
  vpc:
    security_groups: [sg-07b42971d4a13fca3]

# Configuration for your containers and service.
image:
  location: wordpress:5
  # Port exposed through your container to route traffic to it.
  port: 80

cpu: 256       # Number of CPU units for the task.
memory: 512    # Amount of memory in MiB used by the task.
count: 5       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.

storage:
  volumes:
    wpUserData:
      path: /var/www/html/wp-content
      efs: true
      read_only: false


secrets:     # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  WORDPRESS_DB_HOST: /rds/db-endpoint
  WORDPRESS_DB_USER: /rds/db-user
  WORDPRESS_DB_PASSWORD: /rds/db-password
  WORDPRESS_DB_NAME: /rds/db-name
#  GITHUB_TOKEN: GITHUB_TOKEN  # The key is the name of the environment variable, the value is the name of the SSM parameter.

# You can override any of the values defined above by environment.
#environments:
#  test:
#    count: 2               # Number of tasks to run for the "test" environment.
